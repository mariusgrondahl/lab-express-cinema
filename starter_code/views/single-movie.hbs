  <div class="flex-container">
      <div class="details-container">
        <h1>{{movie.title}}</p></h1>
        <p>{{movie.description}}</p>

        <h4>Showtimes:</h4>
        <ul>
          {{#each movie.showtimes}}
            <li>{{this}}</li>
          {{/each}}
         </ul>
        </div>
    </div>
